# FarmMind_interface_offline.py inference environment
# Tested baseline: Python 3.10, CUDA 11.8, NVIDIA GPU
#
# Install from the FarmMind repository root:
#   conda create -n farmmind-interface python=3.10 -y
#   conda activate farmmind-interface
#   python -m pip install --upgrade pip setuptools wheel
#   python -m pip install -r requirements_interface.txt

--extra-index-url https://download.pytorch.org/whl/cu118
#FarmMind_interface_offline.py
# PyTorch and SAM 2
torch==2.5.1+cu118
torchvision==0.20.1+cu118
hydra-core==1.3.2
iopath==0.1.10
ninja==1.11.1.4
tqdm==4.67.1

# Qwen2.5-VL
accelerate==1.5.2
transformers==4.49.0
qwen-vl-utils==0.0.10
av==14.2.0

# Image and geospatial processing
numpy==1.26.4
pillow==11.1.0
opencv-python-headless==4.11.0.86
rasterio==1.4.3

# Flask service and configuration
Flask==3.1.2
PyYAML==6.0.2
requests==2.32.3

# Vendored SAM 2 package
-e ./module/sam2_seg

# ---------------------------------------------------------------------------
# FarmMind_segearth_offline.py: separate SegEarth-R1 environment
# ---------------------------------------------------------------------------
# Do NOT install the SegEarth-R1 dependencies into farmmind-interface.
# FarmMind_segearth_offline.py must run in a second Conda environment configured
# from the official SegEarth-R1 repository:
#
#   https://github.com/earth-insights/SegEarth-R1
#   https://github.com/earth-insights/SegEarth-R1/blob/main/docs/Installation.md
#
# Follow the official installation guide:
#
#   conda create -n farmmind-segearth python=3.10 -y
#   conda activate farmmind-segearth
#   git clone https://github.com/earth-insights/SegEarth-R1.git
#   cd SegEarth-R1
#   python -m pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 \
#     --index-url https://download.pytorch.org/whl/cu121
#   python -m pip install -r requirements.txt
#
# Detectron2 must then be installed by following its official installation
# instructions. Finally, compile the SegEarth-R1 MSDeformAttn CUDA operator:
#
#   cd segearth_r1/model/mask_decoder/Mask2Former_Simplify/modeling/pixel_decoder/ops
#   sh make.sh
#
# Activate farmmind-segearth when running:
#
#   python FarmMind_segearth_offline.py ...
